# DRESS ONLINE MARKET

## Requirements:
  - NODE upper than v19
  - Mongo DB
   
## In order to run project, do below steps:
  - fill out `.env` as shown in `.example.env` file.
  - npm i
  - npm run dev
  - go to your browser and check `http://localhost`.